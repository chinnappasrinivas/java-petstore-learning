image: maven:3.9-eclipse-temurin-17

stages:
  - build
  - unit_test

build:
  stage: build
  script:
    - mvn clean package -DskipTests
  artifacts:
    paths:
      - target/*.jar
    expire_in: 1 hour

unit_test:
  stage: unit_test
  script:
    - mvn test